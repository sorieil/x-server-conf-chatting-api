image: atlassian/default-image:latest
pipelines:
    branches:
        test:
            - step:
                  name: Build & Deploy
                  deployment: test
                  script:
                      - ssh ubuntu@$(echo $DEPLOY_SERVER_IP) 'bash -s' < ./test-run.sh
                      - echo 'Deploy success!!!'
